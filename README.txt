To run this project run the following line in the command line: 
make && ./chash && make clean 

And to remove the .o files run the following line in the command line:
make clean

To run this project and send output to file use the following line: 
make && ./chash > output.txt && make clean 